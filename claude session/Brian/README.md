# Claude Code sessions — Brian

Two logs, one per assignment.

| Log | What it covers |
|---|---|
| [`secondhand-marketplace/`](secondhand-marketplace/) | This project — the Figma screens for the five user flows, the Next.js scaffold on top of them, and the entry/feed/detail/upload pages |
| [`sort-reports/`](sort-reports/) | The `sort-reports` bug hunt — a separate assignment, included so my logs are all in one place |

Each folder has its own README covering how that transcript was produced, how
to read it, and what was masked before publishing. The two differ on one point
worth knowing up front: the marketplace log keeps tool *results*, the
`sort-reports` log lists only the calls.
