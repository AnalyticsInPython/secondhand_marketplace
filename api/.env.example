# Copy to api/.env and fill in. Never commit .env.

# Which addresses may create an account. Config, not code -- opening the pilot
# to another Columbia school is an environment change and a redeploy.
# Agreed set: the university address, Business, the Medical Center, and
# Teachers College.
ALLOWED_EMAIL_DOMAINS=columbia.edu,gsb.columbia.edu,cumc.columbia.edu,tc.columbia.edu

# --- Supabase --------------------------------------------------------------
# Project settings -> API. Use the DEV project locally; prod only on main.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
SUPABASE_JWT_ISSUER=https://<project-ref>.supabase.co/auth/v1

# --- Postgres --------------------------------------------------------------
# Project settings -> Database -> Connection string (session pooler).
DATABASE_URL=postgresql+psycopg://postgres:<password>@<host>:5432/postgres

# --- CORS ------------------------------------------------------------------
# The Vercel domains only. Never "*".
CORS_ORIGINS=http://localhost:3000
